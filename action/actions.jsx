// post request to create a new todo
export const createTodo = (text) => async (dispatch) => {};

// put request to change the status of the todo to completed
export const updateTodo = (id) => async (dispatch) => {};

// delete request to delete the todo
export const deleteTodo = (id) => async (dispatch) => {};

// get request to get the list of uncompleted todos
export const getTodos = () => async (dispatch) => {};

// get request to get all the completed todos
export const getCompletedTodos = () => async (dispatch) => {};
